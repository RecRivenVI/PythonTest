# 自动推测生成的 requirements.txt（未固定版本）
# 说明：
# - 依据仓库中导入语句自动推测可能需要的第三方包。
# - 某些包名称存在歧义或依赖系统库（例如 pyexiv2 可能需要本机 exiv2 开发库），请根据你的运行环境酌情调整并加上版本号。
# - 如果你希望我帮忙自动 pin 版本（例如根据当前虚拟环境或 PyPI 最新版本），告诉我我可以继续执行。

requests
beautifulsoup4
pandas
plotly
dash
Pillow
pyexiv2  # 注意：可能需要系统依赖或替代包（请确认）
tkcalendar
matplotlib
mplcursors
toml
tqdm
numpy
pytz

# 可选/视情况需要安装：
# - opencv-python  (如脚本中使用了 cv2)
# - exiftool（系统命令行工具，某些脚本通过 subprocess 调用 exiftool，而非 Python 包）
